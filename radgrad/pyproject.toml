[project]
name = "radgrad"
version = "0.1.0"
description = "Educational implementation of reverse mode automatic differentiation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.0",
]
