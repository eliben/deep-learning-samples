This is taken from the assignments of http://cs231n.github.io/

The shell script downloads the CIFAR-10 data set for Python. Since it's large,
we don't want to keep it inside git so it's in .gitignore
